# Backend service config for Railway
[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "/docker-entrypoint.sh"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
